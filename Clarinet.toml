[project]
name = "speed-priority"
authors = ["SpeedPriority Team"]
description = "High-velocity carbon credit marketplace on Stacks blockchain"
telemetry = false
requirements = []
[contracts.priority-credit]
path = "contracts/priority-credit.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
